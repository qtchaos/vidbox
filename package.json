{
    "name": "vidbox",
    "type": "module",
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "dev:server": "cd packages/server && bun run index.ts",
        "dev:client": "cd packages/client && bun dev"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    }
}
